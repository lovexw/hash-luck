# 🎲 Hash-Luck - 每日比特币幸运号码抽奖

基于比特币区块链的公平公正抽奖系统，每天从北京时间 12:00 后的第一个比特币区块哈希值中提取幸运号码。

## ✨ 特性

- 🔐 **完全公平**: 基于比特币区块链，无法预测或操纵
- ⏰ **每日更新**: 每天北京时间 12:00 自动抓取数据
- 🌐 **全球访问**: 使用 Cloudflare Workers 部署，快速稳定
- 📊 **历史记录**: 所有历史数据存储在 GitHub，完全透明
- 🎨 **精美界面**: 响应式设计，支持移动端访问

## 🎯 工作原理

1. **数据获取**: 每天北京时间 12:00，GitHub Actions 自动运行
2. **区块筛选**: 获取当天北京时间 12:00 后的第一个比特币区块
3. **号码提取**: 从区块哈希值中提取数字，反转后取前 3 位和前 6 位
4. **数据存储**: 保存为 JSON 文件并提交到 GitHub 仓库
5. **前端展示**: Cloudflare Worker 从 GitHub 拉取数据并展示

## 📁 项目结构

```
.
├── .github/
│   └── workflows/
│       └── fetch_block.yml      # GitHub Actions 工作流
├── scripts/
│   └── fetch_block.js           # 数据获取脚本
├── worker/
│   └── index.html               # Cloudflare Worker 前端
├── data/                        # 数据目录（自动生成）
│   ├── YYYY-MM-DD.json          # 每日数据
│   └── history.json             # 历史汇总
├── package.json                 # Node.js 配置
├── wrangler.toml                # Cloudflare Worker 配置
└── README.md
```

## 🚀 部署指南

### 1. 克隆仓库

```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
```

### 2. 安装依赖

```bash
npm install
```

### 3. 配置 GitHub Actions

GitHub Actions 工作流已配置完成，会在以下情况自动运行：

- 每天 UTC 04:00（北京时间 12:00）
- 手动触发（在 GitHub Actions 页面点击 "Run workflow"）

**注意**: 确保仓库的 Actions 权限已启用：
- 前往仓库设置 → Actions → General
- 在 "Workflow permissions" 中选择 "Read and write permissions"

### 4. 测试数据获取

手动运行脚本测试：

```bash
npm run fetch
```

这将生成当天的数据文件并保存到 `data/` 目录。

### 5. 部署 Cloudflare Worker

#### 方式一：使用 Cloudflare Pages

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 前往 Workers & Pages → Create Application → Pages
3. 连接你的 GitHub 仓库
4. 构建设置：
   - 构建命令: 留空
   - 构建输出目录: `worker`
5. 点击 "Save and Deploy"

#### 方式二：使用 Wrangler CLI

```bash
# 安装 Wrangler
npm install -g wrangler

# 登录 Cloudflare
wrangler login

# 部署
npm run deploy
```

### 6. 更新前端配置

编辑 `worker/index.html`，将以下变量替换为你的实际值：

```javascript
const GITHUB_RAW_BASE = 'https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/main/data';
```

替换为：

```javascript
const GITHUB_RAW_BASE = 'https://raw.githubusercontent.com/你的用户名/你的仓库名/main/data';
```

## 📊 数据格式

### 每日数据文件 (YYYY-MM-DD.json)

```json
{
  "date": "2025-12-01",
  "block_height": 870000,
  "block_hash": "0000000000000000000123456789abcdef",
  "block_timestamp": 1733029200,
  "lucky3": "890",
  "lucky6": "567890",
  "generated_at": "2025-12-01T04:15:00.000Z"
}
```

### 历史汇总文件 (history.json)

```json
[
  {
    "date": "2025-12-01",
    "block_height": 870000,
    "block_hash": "0000000000000000000123456789abcdef",
    "block_timestamp": 1733029200,
    "lucky3": "890",
    "lucky6": "567890",
    "generated_at": "2025-12-01T04:15:00.000Z"
  }
]
```

## 🔢 幸运号码提取算法

```javascript
// 示例区块哈希
const blockHash = "0000000000000000000123456789abcdef";

// 1. 提取所有数字
const digits = "0000000000000000000123456789";

// 2. 反转数字序列
const reversed = "9876543210000000000000000000";

// 3. 取前 3 位和前 6 位
const lucky3 = "987";
const lucky6 = "987654";
```

## 🛠️ 开发

### 本地运行前端

```bash
# 启动 Wrangler 开发服务器
npm run dev
```

访问 http://localhost:8788 查看前端效果。

### 手动触发数据获取

```bash
npm run fetch
```

### API 端点

项目使用 [Blockstream.info API](https://blockstream.info/api/):

- 获取最近区块: `https://blockstream.info/api/blocks`
- 区块详情: `https://blockstream.info/api/block/{hash}`

## 📅 定时任务

GitHub Actions 工作流配置为每天 UTC 04:00（北京时间 12:00）自动运行。

Cron 表达式: `0 4 * * *`

你也可以在 GitHub Actions 页面手动触发工作流。

## 🎨 前端特性

- 响应式设计，支持移动端
- 渐变色彩，金色/紫色主题
- 数字动画效果
- 悬停交互效果
- 自动加载今日和历史数据
- 友好的错误提示

## 📝 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## ⚠️ 免责声明

本项目仅供学习和娱乐用途，不构成任何形式的赌博或投资建议。使用本系统进行任何商业或赌博活动，后果自负。

## 📞 联系方式

如有问题或建议，请在 GitHub 上提交 Issue。

---

⭐ 如果觉得这个项目有用，欢迎 Star！
